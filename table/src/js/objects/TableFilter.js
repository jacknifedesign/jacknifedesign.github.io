(function( $ ) {
	var TableFilter = JACKNIFE.TableFilter = function TableFilter(el) {
		// Elements
		var $el = $(el);

		// Variables

		// Public Functions

		this.init = function() {
		}

		// Private Functions
	}
})( jQuery );